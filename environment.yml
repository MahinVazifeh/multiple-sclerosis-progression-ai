name: msss-prediction
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib

  - pip:
      - tensorflow
      - autogluon